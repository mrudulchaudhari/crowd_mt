# Core
Django==5.0.3
djangorestframework==3.15.1

# WebSockets (Channels)
channels==4.0.0
# Uncomment the next line only if you plan to use Redis (not needed for simple demo)
# channels-redis==4.2.0

# ML
scikit-learn==1.4.2
joblib==1.3.2

# Deployment helpers (optional, useful for Render/Heroku)
gunicorn==21.2.0
whitenoise==6.6.0

# Database (optional: only if using Postgres for deployment)
# psycopg2-binary==2.9.9
asgiref==3.9.1
channels==4.3.1
Django==5.2.6
djangorestframework==3.16.1
sqlparse==0.5.3
typing_extensions==4.15.0
tzdata==2025.2
